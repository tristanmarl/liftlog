# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
npm run dev       # start dev server (Vite HMR)
npm run build     # tsc + vite build
npm run lint      # eslint
npm run preview   # preview dist build
```

No test suite exists yet.

## Environment

Requires `.env` with:
```
VITE_HEVY_API_KEY=your-api-key-here
VITE_LIFTOSAUR_API_TOKEN=your-token-here   # enables live Liftosaur fetch via Vite proxy
```

Liftosaur data is fetched live via the Vite dev-server proxy (`/liftosaur-api` → `liftosaur.com/api/v1`) when `VITE_LIFTOSAUR_API_TOKEN` is set. Without it, falls back to `public/liftosaur-history.json` (generated by `node scripts/sync-liftosaur-history.mjs`).

## Architecture

Single-page React app (Vite + TypeScript + Tailwind) that visualises workout data from Hevy or Liftosaur. Keep the dependency footprint small: charts use local SVG via `src/components/SimpleChart.tsx`, dates use `src/utils/date.ts`, and there is no chart/date/classname helper dependency.

**Types:** `src/types/workout.ts` is the canonical runtime shape (`Workout`, `WorkoutExercise`, `WorkoutSet`, `BodyweightEntry`). `src/types/hevy.ts` holds raw Hevy API response shapes; the API layer maps them to the canonical types.

**Data sources:** `src/api/dataSource.ts` is the single entry point for all data. It dispatches to `src/api/hevy.ts` (live REST API, in-memory + sessionStorage cache) or `src/api/liftosaur.ts` (live proxy or `public/liftosaur-history.json`, sessionStorage cache). Liftosaur workouts are read-only. Active source (`hevy` | `liftosaur`) is persisted to `localStorage` via `DataVersionContext`.

**State:** `src/context/DataVersion.tsx` holds the active `DataSource` and a `version` counter. Calling `refresh()` clears the cache and bumps `version`, which causes pages to re-fetch. No other global state.

**Computation layer:** `src/utils/stats.ts` — analytics (weekly/period stats, PRs, muscle balance, plateaus, streaks, routine progression suggestions) as pure functions over `Workout[]`. `src/utils/muscles.ts` infers muscle groups via keyword matching on exercise title and API-provided muscle groups.

**Routing:** React Router v7, all routes under a single `Layout` shell with a collapsible sidebar and `⌘K` global search. Period selection (`week / 30d / 90d / 365d`) is persisted to `localStorage`.

**Styling:** Tailwind utility classes + occasional inline `style` props for brand orange `#e86a2e` and dark palette (`#0f0f0f` bg, `#1a1a1a` cards, `#2a2a2a` borders). No CSS modules or styled-components.

## Data Safety

Never commit `.env`, `public/liftosaur-history.json`, raw workout exports, `dist/`, or screenshots containing personal data. Check `git status --ignored` before committing.
